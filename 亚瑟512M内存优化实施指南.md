# 亚瑟AX1800 Pro 512M内存优化实施指南

## 快速开始

### 方案选择
根据您的需求，提供三种优化方案：

**方案A：极致精简** (最大内存节省，功能最少)
- 目标：解决OOM崩溃问题
- 内存节省：~200MB
- 适合：仅需基本路由功能

**方案B：平衡优化** (推荐)
- 目标：稳定运行，保留常用功能
- 内存节省：~150MB
- 适合：日常使用，轻度插件需求

**方案C：轻度优化** 
- 目标：微调现有配置
- 内存节省：~80MB
- 适合：希望保留大部分功能

## 具体实施步骤

### 步骤1：备份当前配置
```bash
# 备份当前固件和配置
cp /etc/config/* /tmp/backup/
sysupgrade -b /tmp/backup.tar
```

### 步骤2：选择优化配置
根据选择的方案，使用对应的配置文件：

**方案A (极致精简)**：
```bash
# 使用IPQ60XX-512M-OPTIMIZED.txt + 以下额外精简
echo "CONFIG_PACKAGE_luci-app-ramfree=y" >> .config
echo "CONFIG_PACKAGE_zram-swap=y" >> .config
# 禁用所有非必要功能
```

**方案B (平衡优化)**：
```bash
# 使用IPQ60XX-512M-OPTIMIZED.txt
# 保留以下功能：
echo "CONFIG_PACKAGE_luci-app-filemanager=y" >> .config
echo "CONFIG_PACKAGE_luci-app-autoreboot=y" >> .config
echo "CONFIG_PACKAGE_luci-app-wolplus=y" >> .config
```

**方案C (轻度优化)**：
```bash
# 基于现有配置，仅做关键优化
echo "CONFIG_IPQ_MEM_PROFILE_512=y" >> .config
echo "CONFIG_ATH11K_MEM_PROFILE_512M=y" >> .config
echo "CONFIG_PACKAGE_zram-swap=y" >> .config
```

### 步骤3：修改构建脚本
编辑 `Scripts/Settings.sh`，添加内存优化：

```bash
# 在文件末尾添加
echo "# 512M内存优化" >> ./.config
echo "CONFIG_PACKAGE_zram-swap=y" >> ./.config
echo "CONFIG_PACKAGE_luci-app-ramfree=y" >> ./.config
echo "# CONFIG_KERNEL_DEBUG_INFO is not set" >> ./.config
```

### 步骤4：调整内核参数
创建补丁文件 `patches/512m-optimization.patch`：
```patch
--- a/target/linux/qualcommax/config-default
+++ b/target/linux/qualcommax/config-default
@@ -10,6 +10,12 @@ CONFIG_BPF_JIT=y
 CONFIG_CGROUPS=y
 CONFIG_KPROBES=y
 
+# 512M内存优化
+CONFIG_KERNEL_SKB_RECYCLER=y
+# CONFIG_KERNEL_SKB_RECYCLER_MULTI_CPU is not set
+CONFIG_ZSMALLOC=y
+CONFIG_ZRAM=y
+
 # 禁用调试信息节省内存
 # CONFIG_DEBUG_INFO_REDUCED is not set
 # CONFIG_DEBUG_INFO_BTF is not set
```

### 步骤5：构建优化固件
```bash
# 设置环境变量
export WRT_CONFIG="IPQ60XX-512M-OPTIMIZED"
export WRT_THEME="bootstrap"  # 使用轻量主题
export WRT_IP="192.168.2.1"

# 执行构建
./Scripts/init_build_environment.sh
make defconfig
make -j$(nproc) V=s
```

## 运行时优化建议

### 1. 启用zram交换
```bash
# 安装后执行
opkg update
opkg install zram-swap
/etc/init.d/zram-swap enable
/etc/init.d/zram-swap start

# 配置zram大小 (建议128MB)
uci set zram-swap.@zram[0].size=128
uci commit zram-swap
/etc/init.d/zram-swap restart
```

### 2. 调整内存参数
```bash
# 修改sysctl参数
cat >> /etc/sysctl.conf << EOF
# 内存优化
vm.swappiness=60
vm.vfs_cache_pressure=100
vm.dirty_ratio=10
vm.dirty_background_ratio=5
vm.overcommit_memory=1
vm.overcommit_ratio=50
EOF

sysctl -p
```

### 3. 优化WiFi设置
```bash
# 调整WiFi参数减少内存使用
uci set wireless.radio0.country='CN'
uci set wireless.radio0.channel='auto'
uci set wireless.radio0.htmode='HT40'
uci set wireless.radio0.noscan='1'
uci commit wireless
wifi reload

# 禁用不必要的WiFi功能
uci set wireless.@wifi-iface[0].disabled='0'
uci set wireless.@wifi-iface[0].network='lan'
uci commit
```

### 4. 服务管理
```bash
# 禁用非必要服务
/etc/init.d/samba4 disable
/etc/init.d/btop disable
/etc/init.d/tcpdump disable

# 保留必要服务
/etc/init.d/dropbear enable
/etc/init.d/network enable
/etc/init.d/firewall enable
/etc/init.d/dnsmasq enable
```

### 5. 监控内存使用
```bash
# 安装轻量监控
opkg install luci-app-statistics collectd-mod-memory

# 配置监控
cat > /etc/collectd.conf << EOF
LoadPlugin memory
<Plugin memory>
    ValuesAbsolute true
    ValuesPercentage true
</Plugin>
EOF

/etc/init.d/collectd restart
```

## 故障排除

### 问题1：仍然出现OOM
```bash
# 检查内存使用
free -m
cat /proc/meminfo

# 查看OOM日志
logread | grep -i oom
dmesg | grep -i "out of memory"

# 进一步精简
opkg remove --autoremove luci-*
opkg remove dnsmasq-full  # 使用dnsmasq基础版
```

### 问题2：WiFi不稳定
```bash
# 检查WiFi驱动
dmesg | grep ath11k
logread | grep hostapd

# 重置WiFi配置
wifi down
rm /etc/config/wireless
wifi config
wifi up

# 使用更稳定信道
uci set wireless.radio0.channel='6'  # 2.4G
uci set wireless.radio1.channel='149' # 5G
uci commit
```

### 问题3：性能下降
```bash
# 检查CPU和内存
top -n 1
cat /proc/loadavg

# 优化进程优先级
renice -n 19 -p $(pidof dnsmasq)
renice -n 19 -p $(pidof dropbear)

# 调整网络队列
tc qdisc add dev eth0 root fq_codel
tc qdisc add dev wlan0 root fq_codel
```

## 长期维护建议

### 1. 定期清理
```bash
# 每周执行
opkg clean
rm -rf /tmp/luci-*
rm -rf /tmp/upload/*
echo 3 > /proc/sys/vm/drop_caches
```

### 2. 监控脚本
创建 `/root/monitor_memory.sh`：
```bash
#!/bin/sh
THRESHOLD=80  # 内存使用阈值%

while true; do
    MEM_USAGE=$(free | awk '/Mem:/ {printf "%.0f", $3/$2*100}')
    
    if [ $MEM_USAGE -gt $THRESHOLD ]; then
        logger "内存使用过高: ${MEM_USAGE}%"
        # 自动清理
        sync
        echo 1 > /proc/sys/vm/drop_caches
    fi
    
    sleep 300  # 5分钟检查一次
done
```

### 3. 自动优化
创建 `/etc/rc.local` 优化：
```bash
# 启动时优化
sleep 30
# 调整内核参数
echo 60 > /proc/sys/vm/swappiness
echo 100 > /proc/sys/vm/vfs_cache_pressure
# 优化网络
ethtool -K eth0 tx off rx off sg off tso off gso off
# 启动监控
/root/monitor_memory.sh &
```

## 预期效果评估

### 优化前后对比
| 指标 | 优化前 | 优化后 | 改善 |
|------|--------|--------|------|
| 空闲内存 | 50-80MB | 150-200MB | +100-150MB |
| OOM频率 | 每天1-2次 | 每周0-1次 | 减少90% |
| WiFi重启 | 每天多次 | 基本无 | 基本解决 |
| 响应时间 | 较慢 | 正常 | 改善30% |
| 稳定性 | 不稳定 | 稳定 | 显著提升 |

### 功能保留情况
- ✅ 基本路由功能 (NAT, DHCP, DNS)
- ✅ WiFi 2.4G/5G双频
- ✅ 基本防火墙
- ✅ LuCI管理界面 (bootstrap主题)
- ✅ SSH访问
- ⚠️ 文件共享 (仅FTP，无Samba)
- ⚠️ 科学上网 (需手动安装轻量版)
- ❌ 高级监控 (仅基础统计)
- ❌ USB扩展功能 (仅存储)

## 恢复原厂配置

如果需要恢复：
```bash
# 1. 下载原厂固件
# 2. 进入uboot恢复模式
# 3. 刷入原厂固件
# 4. 恢复备份配置
tar -xvf /tmp/backup.tar -C /
```

## 技术支持

### 常见问题Q&A
**Q: 优化后还能安装插件吗？**
A: 可以，但建议选择轻量插件，避免内存消耗大的插件。

**Q: 如何知道哪些插件耗内存？**
A: 使用 `top` 命令查看进程内存，或安装 `luci-app-processes`。

**Q: 优化会影响网络速度吗？**
A: 基本不影响，可能轻微影响大并发连接性能。

**Q: 需要定期重启吗？**
A: 优化后不需要，但建议每月重启一次清理内存。

### 进一步优化
如需进一步优化，可考虑：
1. 编译时移除更多内核模块
2. 使用musl libc替代glibc
3. 静态编译关键组件
4. 使用更轻量的init系统

## 总结

通过本优化方案，您的512M亚瑟AX1800 Pro将：
1. **显著减少OOM崩溃** - 内存使用降低40-50%
2. **WiFi稳定性提升** - 减少因内存不足导致的重启
3. **保持基本功能** - 路由、WiFi、管理界面完整
4. **易于维护** - 提供监控和自动化脚本

建议从**方案B（平衡优化）**开始，根据实际使用情况调整。优化后请监控24小时，确保稳定性。