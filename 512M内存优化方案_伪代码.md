# 亚瑟AX1800 Pro 512M内存优化方案 - 模块化伪代码

## 问题分析
- **设备**: 京东云亚瑟AX1800 Pro (IPQ6018平台)
- **内存**: 512MB (实际可用约450MB)
- **问题**: 频繁OOM崩溃，WiFi重启
- **根本原因**: 内存消耗过大，内核配置未针对512M优化

## 优化策略

### 1. 内存配置优化
```
PROCEDURE configure_memory_profile():
    INPUT: 目标内存大小 (512M)
    PROCESS:
        1. 设置IPQ内存配置:
           - CONFIG_IPQ_MEM_PROFILE_512=y
           - CONFIG_IPQ_MEM_PROFILE_1024=n
        
        2. 设置WiFi内存配置:
           - CONFIG_ATH11K_MEM_PROFILE_512M=y
           - CONFIG_ATH11K_MEM_PROFILE_1G=n
        
        3. 启用内存回收:
           - CONFIG_KERNEL_SKB_RECYCLER=y
           - 单CPU模式节省内存
        
        4. 配置zram交换:
           - CONFIG_PACKAGE_zram-swap=y
           - 使用压缩交换缓解内存压力
    OUTPUT: 优化的内存配置
```

### 2. 内核精简优化
```
PROCEDURE optimize_kernel_config():
    INPUT: 当前内核配置
    PROCESS:
        1. 禁用非必要cgroup功能:
           - 禁用HUGETLB (大页内存)
           - 禁用NET_CLS_CGROUP (网络分类)
           - 禁用MEMCG_SWAP (内存控制组交换)
        
        2. 精简调度器:
           - 禁用CFQ_GROUP_IOSCHED
           - 保持基本CFQ调度
        
        3. 禁用调试信息:
           - 禁用DEBUG_INFO (节省约50MB)
           - 禁用DEBUG_INFO_BTF
           - 禁用DEBUG_INFO_REDUCED
        
        4. 优化RCU配置:
           - 禁用PREEMPT_RCU (减少上下文切换)
    OUTPUT: 精简的内核配置
```

### 3. WiFi稳定性优化
```
PROCEDURE optimize_wifi_for_512m():
    INPUT: WiFi当前配置
    PROCESS:
        1. 选择稳定NSS版本:
           - CONFIG_NSS_FIRMWARE_VERSION_12_2=y (稳定)
           - 禁用12.5 (实验性)
        
        2. 精简WiFi固件:
           - 仅保留ipq6018固件
           - 删除其他设备固件 (qcn9074等)
        
        3. 优化驱动配置:
           - 保持基本ath11k驱动
           - 禁用非必要调试功能
        
        4. 调整WiFi参数:
           - 降低扫描频率
           - 优化信道选择算法
    OUTPUT: 稳定的WiFi配置
```

### 4. 服务与包精简
```
PROCEDURE remove_memory_hungry_services():
    INPUT: 当前包列表
    PROCESS:
        1. 禁用科学上网插件:
           - 禁用dae, daed (各节省20-30MB)
           - 禁用passwall, mosdns
        
        2. 精简文件服务:
           - 禁用samba4 (节省15-20MB)
           - 禁用filemanager (节省10MB)
        
        3. 移除监控工具:
           - 禁用btop, htop (节省5-10MB)
           - 禁用tcpdump
        
        4. 精简USB支持:
           - 仅保留基本USB存储
           - 禁用USB网络、音频、打印
        
        5. 优化LuCI:
           - 使用bootstrap主题 (轻量)
           - 禁用argon主题 (节省5MB)
           - 移除非必要LuCI应用
    OUTPUT: 精简的包列表
```

### 5. 网络栈优化
```
PROCEDURE optimize_network_stack():
    INPUT: 网络配置
    PROCESS:
        1. 精简nftables:
           - 仅保留core和nat模块
           - 禁用bridge, fib, fullcone等扩展
        
        2. 优化TCP配置:
           - 使用cubic拥塞控制 (稳定)
           - 调整TCP缓冲区大小
        
        3. 减少连接跟踪:
           - 降低conntrack表大小
           - 缩短超时时间
        
        4. 禁用eBPF:
           - 完全禁用BPF相关功能
           - 节省约30-50MB内存
    OUTPUT: 轻量网络栈
```

## 完整优化流程伪代码

```
MAIN OPTIMIZATION_PIPELINE():
    // 阶段1: 分析当前状态
    current_mem_usage = analyze_memory_usage()
    oom_patterns = identify_oom_patterns()
    wifi_issues = analyze_wifi_stability()
    
    // 阶段2: 应用配置优化
    IF current_mem_usage > 400MB THEN
        configure_memory_profile(512)
        optimize_kernel_config()
        optimize_wifi_for_512m()
    ENDIF
    
    // 阶段3: 精简服务
    memory_hungry_services = identify_heavy_services()
    FOR EACH service IN memory_hungry_services:
        IF service.priority == "low" THEN
            disable_service(service)
        ENDIF
    ENDFOR
    
    // 阶段4: 网络优化
    optimize_network_stack()
    
    // 阶段5: 验证优化
    new_mem_usage = measure_memory_usage()
    stability_score = test_wifi_stability(24h)
    
    // 阶段6: 生成报告
    generate_optimization_report(
        memory_saved = current_mem_usage - new_mem_usage,
        stability_improvement = stability_score,
        features_lost = list_disabled_features()
    )
```

## 具体配置修改示例

### 示例1: 修改GENERAL.txt精简版
```
# 512M优化版GENERAL.txt (精简)
CONFIG_PACKAGE_luci-app-ramfree=y
CONFIG_PACKAGE_luci-theme-bootstrap=y
CONFIG_PACKAGE_luci-app-autoreboot=y
CONFIG_PACKAGE_luci-app-wolplus=y
CONFIG_PACKAGE_luci-app-ttyd=y

# 禁用内存消耗大的功能
# CONFIG_PACKAGE_luci-app-daed is not set
# CONFIG_PACKAGE_luci-app-dae is not set
# CONFIG_PACKAGE_luci-app-samba4 is not set
# CONFIG_PACKAGE_luci-app-filemanager is not set
# CONFIG_PACKAGE_btop is not set
# CONFIG_PACKAGE_iperf3 is not set

# 内核优化
CONFIG_PACKAGE_kmod-tcp-bbr=y
CONFIG_DEFAULT_tcp_cubic=y
CONFIG_PACKAGE_zram-swap=y
```

### 示例2: 构建脚本修改
```
PROCEDURE build_512m_optimized_firmware():
    // 设置环境变量
    SET WRT_CONFIG="IPQ60XX-512M-OPTIMIZED"
    SET WRT_ARCH="qualcommax_ipq60xx"
    SET WRT_THEME="bootstrap"  // 使用轻量主题
    SET WRT_IP="192.168.2.1"
    
    // 修改内核大小 (保持12M但内容精简)
    EXECUTE set_kernel_size()  // 来自function.sh
    
    // 应用优化配置
    COPY Config/IPQ60XX-512M-OPTIMIZED.txt TO .config
    APPEND Config/GENERAL-512M.txt TO .config
    
    // 构建
    RUN make defconfig
    RUN make -j$(nproc) V=s
```

## 预期优化效果

### 内存节省估算
```
MEMORY_SAVINGS_ESTIMATE:
    1. 内核调试信息: 50MB
    2. eBPF相关功能: 30MB
    3. 科学上网插件: 40MB (dae + daed)
    4. 文件服务: 25MB (samba4 + filemanager)
    5. 监控工具: 10MB
    6. USB扩展功能: 15MB
    7. LuCI主题: 5MB (argon → bootstrap)
    ----------------------------
    总计节省: ~175MB
    
预期内存使用:
    优化前: 400-450MB (接近OOM)
    优化后: 225-275MB (安全范围)
```

### WiFi稳定性改进
```
WIFI_IMPROVEMENTS:
    1. 减少OOM导致的WiFi重启
    2. 更稳定的NSS 12.2驱动
    3. 降低内存压力，减少丢包
    4. 优化信道选择算法
```

## 部署与验证

### 部署步骤
```
DEPLOYMENT_STEPS:
    1. 备份当前配置
    2. 应用优化配置
    3. 重新编译固件
    4. 刷入测试
    5. 监控24小时稳定性
    6. 根据反馈微调
```

### 验证指标
```
VALIDATION_METRICS:
    1. 内存使用率: <70% 长期
    2. OOM事件: 0次/周
    3. WiFi重启: 0次/周
    4. 网络延迟: <50ms 内网
    5. 吞吐量: >300Mbps (有线), >200Mbps (无线)
```

## 注意事项

### 功能牺牲
```
FEATURES_SACRIFICED:
    1. 科学上网插件 (可手动安装轻量替代)
    2. 高级文件共享 (保留基本FTP)
    3. 系统监控工具 (保留基本状态查看)
    4. USB扩展功能 (保留基本存储)
    5. 高级网络功能 (保留基本路由)
```

### 恢复选项
```
RECOVERY_OPTIONS:
    1. 保留备份配置
    2. 提供功能模块化启用脚本
    3. 支持按需安装包
    4. 可回滚到标准版本
```

## 总结

此优化方案针对512M内存的亚瑟AX1800 Pro，通过：
1. **内核精简** - 移除调试和非必要功能
2. **服务裁剪** - 禁用内存消耗大的服务
3. **网络优化** - 简化网络栈配置
4. **WiFi稳定** - 优化驱动和参数

预计可减少约175MB内存使用，显著降低OOM发生概率，提高WiFi稳定性，同时保持基本路由功能完整。